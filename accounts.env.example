# Copy to accounts.env (gitignored). Codes from https://cub.rip/add — one per account slot.
CUB_CODE_SRC=123456
CUB_CODE_TARGET=654321

# Default account slot for CLI commands (src or target). Optional — falls back to src.
CUB_ACCOUNT=src
